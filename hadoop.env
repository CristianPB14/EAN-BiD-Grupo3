# hadoop.env · configuración compartida del clúster HDFS + YARN
# Sin secretos: por eso sí se versiona (ver la excepción en .gitignore).
# Fusiona la configuración de S3 (HDFS) y S4 (YARN/MapReduce), ya que
# docker-compose.yml de este repo trae ambas capas en un solo archivo.

# --- HDFS (sesión 3) ---
CORE_CONF_fs_defaultFS=hdfs://namenode:9000
CORE_CONF_hadoop_http_staticuser_user=root
HDFS_CONF_dfs_replication=3
HDFS_CONF_dfs_permissions_enabled=false
HDFS_CONF_dfs_webhdfs_enabled=true
HDFS_CONF_dfs_namenode_datanode_registration_ip___hostname___check=false

# --- YARN / MapReduce (sesión 4) ---
YARN_CONF_yarn_resourcemanager_hostname=resourcemanager
MAPRED_CONF_mapreduce_framework_name=yarn
