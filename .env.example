# Credenciales de PostgreSQL para docker-compose-jupyter.yml
# Copie este archivo a .env y complete los valores.
# .env NUNCA se sube al repositorio.
POSTGRES_USER=
POSTGRES_PASSWORD=
POSTGRES_DB=
MINIO_ROOT_USER=
MINIO_ROOT_PASSWORD=